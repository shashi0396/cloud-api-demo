### git commands

git clone https://github.com/shashi0396/cloud-api-demo.git

### to add single file
git add file-name

### to add mutiple file
git add .

### check the status
git status

### commit the changes to local repo
git commit -m "commit message"

### push the files to remote repo
git push

### push -- rejected dure to drift in remote repo
git pull
git push